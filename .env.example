# EcoRoute AI — API Keys
# Copy this file to ".env" (same folder) and paste your keys below.
# The app reads keys from here automatically — there is no key input in the UI.

OPENWEATHER_API_KEY=
GEMINI_API_KEY=
